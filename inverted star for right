#include <stdio.h>
int main() 
{
  int n,rows,cols;
  printf("enter a number:");
  scanf("%d",&n);
  for(rows=n;rows>=1;rows--)
  {
      for(cols=1;cols<=rows;cols++)
      {
         printf("* ");
      }
      printf("\n");
  }
